import React from 'react'

const VideoTemp = ({chapName , image}) => {
  return (
    <div>
        <div className="video-card">
            <h3>{chapName}</h3>
        </div>
    </div>
  )
}

export default VideoTemp